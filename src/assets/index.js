// Arquivo responsável por juntar todos os assets e permitir a exportação para o resto do projeto usando desetruturação.

// Caso seja necessário importar algo do assets, basta fazer como no exemplo abaixo.
// import { colors, strings } from 'caminho para a pasta assets'.

import fonts from './values/fonts';
import colors from './values/colors';
import metrics from './values/metrics';
import strings from './values/strings';

export {fonts, colors, metrics, strings};
